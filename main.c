#include <stdio.h>

int main() {
    printf("Hello this is modified code GitHub\n");
    printf("this is in dev branch");
    printf("this is in dev branch, from vs code editor on windows");    
    pritnf("this is in dev branch, from vs code editor on windows, second commit");
    return 0;
}
